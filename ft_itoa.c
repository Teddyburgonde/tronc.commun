#include <stdlib.h>
#include <stdio.h>
#include <string.h>

// convert interger to string

char	*ft_itoa(int n)
{
	if ( n >= '0' && n <= '9')
	{
				
	}
}


int	main(void)
{
	int		c;
	char	buf[20];

	c = 15;
	sprintf(buf, "%d", c);
	printf("%s", buf);
}
